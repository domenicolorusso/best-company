import React from 'react'

function Btn(props) {
    const btnNames = {
        
    }
    return (
        <div className='Btn'>
           
            <button>{props.btnName}</button> 
           
        </div>
    )
}

export default Btn
