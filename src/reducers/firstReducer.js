import {    FIRST_ACTION, SECOND_ACTION } from '../actions/types'


const initialState = {
    firstElement: [],
    secondElement: {}
}

export default function firstReducer( state = initialState, action){
    switch (action.type) {
     default: 
     return state;
}}